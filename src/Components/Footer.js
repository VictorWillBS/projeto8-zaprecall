import React from "react"

export default function Footer({replied,arrQuestions,anwsersIcons,iconClass,children}){
    //Logic
    //UI
    return( 
    <footer>
        
    {children}
    </footer>)
}